/**
 * Atomic UI component library — public barrel export.
 *
 * Always import from this path, never from sub-modules directly:
 *   import { Screen, Card, Typography } from '../components/UI';
 */

export { Screen }      from './Screen';
export type { ScreenProps }     from './Screen';

export { Card }        from './Card';
export type { CardProps, CardVariant } from './Card';

export { Typography }  from './Typography';
export type { TypographyProps, TypographyVariant, TypographyColor } from './Typography';
